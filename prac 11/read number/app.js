"use strict";

function readNumber() {
    let number = prompt("Enter your number");

    while (!isFinite(number))
  
    if (number == null || number == '') 
    return null;
    
  }
  alert(number);


  /** namber ra nemishenasad. ? */
  